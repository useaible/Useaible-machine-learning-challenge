function RunCycleParams(sessionId, inputs, outputs, learn) {

    var self = this;

    self.SessionId = sessionId;
    self.Inputs = inputs;
    self.Outputs = outputs;
    self.Learn = learn;

}